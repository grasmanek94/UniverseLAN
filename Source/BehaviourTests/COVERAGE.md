# BehaviourTests Coverage

Read [`README.md`](README.md), [`BEHAVIOUR_TEST_PLAN.md`](BEHAVIOUR_TEST_PLAN.md),
and [`DEVELOPER_GUIDE.md`](DEVELOPER_GUIDE.md) before expanding this register.
Current milestones and future work are in [`TODO_PROGRESS.md`](TODO_PROGRESS.md).

| Scenario | SDK | State | Public observations | Comparison rule |
| --- | --- | --- | --- | --- |
| `game-server-p2p-sender-identity` | All builds with factory local-init, server-networking, and polling APIs | Implemented; feature-gated characterization | A lobby owner sends a bounded reliable direct reply through public server networking; the client polls once per available packet. | Requires delivery, opaque payload equality, sender=lobby-owner, and sender!=self. No raw ID/type classification, version assumption, or product internals are used. |
| `Simple/automatic-lobby-member-persona` | x64 `1.100.2`, `1.152.11` | Implemented; official comparison is opt-in | Two approved users create and enter one temporary public capacity-two FCM lobby. Both register global lobby-member and persona listeners before controlled discovery/join. The creator requires the remote `ENTERED` member relation, a matching remote persona callback, and a nonempty copied remote persona name. | The host never calls `RequestUserInformation`. IDs, names, marker values, controls, timestamps, credentials, and runtime output are excluded. The compatibility host uses global listeners, validity-only ID checks, and legacy operation forms where `1.100.2` lacks typed IDs, per-request lobby listeners, and user-information retrieve listeners. Both lanes must leave and acknowledge cleanup. |
| Listener P2P configuration | x64 `1.152.11` | Local overlay propagation implemented; official client-redist prerequisite unresolved | `UNIVERSELAN_BEHAVIOUR_TEST_GOG_PEER_OVERLAY` reaches every registered CTest command and stages its `GalaxyPeer64.dll` over the side-by-side copy; staging remains unchanged when unset. | Exact listener delivery and two callback-local peeks are required in both lanes. The verified 2026-09-14 run staged the expected hash but completed 18/21, with all three listener contracts retaining the non-target callback. GOG documents that modern SDKs load the peer from installed client redistributables, so no local-overlay delivery claim is made. |
| `Simple/initialize-and-sign-in` | x64 `1.152.11` | Implemented; live official run remains opt-in | Init return, auth terminal callback/outcome, `SignedIn`, `IsLoggedOn`, self ID validity/type/equality, and persona availability | Exact ordered four-record contract per lane; opaque IDs are lane-local symbols only; account-specific persona text is intentionally not captured; every other record fails. |
| `Simple/session-id-repeatability` | x64 `1.152.11` | Implemented; live official run remains opt-in | After successful `IAuthListener::OnAuthSuccess` and before another `ProcessData`, two consecutive public `IUser::GetSessionID()` calls | Exact ordered five-record contract per lane. The strict `session-id-repeatability` record contains only `equal`; successful sign-in and same-profile UniverseLAN/official boolean equality are required, with no source or manifest assumption that the relation is `true` or `false`. Raw session IDs, zero/nonzero classification, user IDs, persona text, credentials, timestamps, and internals are never recorded. |
| `Simple/gog-services-state` | x64 `1.152.11` | Implemented, accepted beneficial difference | The same sign-in path and self-state observations, then public `IUtils::GetGogServicesConnectionState()` immediately after auth success | Exact ordered five-record contract per same-profile lane pair, except the manifest-declared pair `GOG undefined` and `UniverseLAN connected`, which is classified as `accepted-difference`. It is accepted because the official lane has no GOG Galaxy service while UniverseLAN provides its LAN service. Any other state mismatch or any other record mismatch fails. No service-state listener or order rule is registered. |
| `Simple/gog-services-state` characterization | x64 `1.152.11` | Implemented, explicit runner diagnostic only | The same topology and sign-in path, with immediate, one-`ProcessData`, and ten-pump settled `IUtils::GetGogServicesConnectionState()` queries and symbolic `GlobalGogServicesConnectionStateListener` events phased as `before`, `immediate`, or `settled` | Not a CTest contract and not a comparator: no UniverseLAN/GOG equality or total callback-order assertion. Successful runs retain raw and normalized lane reports; no raw IDs, credentials, or timestamps are recorded. |
| `Simple/stats-retrieve-achievements-number` characterization | x64 `1.152.11` | Implemented; two-lane characterization on 2026-09-16 | After sign-in, each host calls public `IStats::RequestUserStatsAndAchievements` for default self, waits for one terminal callback, compares the callback user with public self, and calls `GetAchievementsNumber` only after success. | Not a CTest contract. It retains only terminal/self-callback/query-completed relations; the returned count and all achievement catalog contents are excluded because they are title configuration. A future title-fixture probe may compare only symbolic unlocked-state relations. Raw IDs, achievement names, descriptions, stat values, credentials, and runtime data are omitted. |
| `Simple/stats-retrieve-self-callback` | x64 `1.152.11` | Passed two-lane comparison on 2026-09-16 (1.37s) | After sign-in, each host calls public `IStats::RequestUserStatsAndAchievements` for default self and observes one terminal callback user relation. | Exact same-profile records require successful retrieval and callback-user-equals-public-self in both lanes. It does not query or compare achievement counts, names, or stat values. Raw IDs, credentials, and runtime data are omitted. |
| `Simple/storage-downloaded-shared-file-count` | x64 `1.152.11` | Passed two-lane comparison on 2026-09-16 (1.19s) | After sign-in and before any shared-file operation, each host calls public `IStorage::GetDownloadedSharedFileCount` once. | Exact same-profile records require `Storage()` availability and no open downloaded shared files in each isolated host. No download, close, read, write, delete, share, synchronization, or enumeration call is issued. Raw IDs, shared-file names, data, credentials, and runtime data are omitted. |
| `Simple/current-game-language` | x64 `1.152.11` | Passed two-lane comparison on 2026-09-16 (1.21s), after two characterizations | After sign-in, each host calls public `IApps::GetCurrentGameLanguage()` once with its default base-game product ID. | Exact same-profile records require `IApps` availability, a non-null result, and no ASCII uppercase character, the safely retained form of the documented lowercase-language property. Language text and pointer values are title/environment configuration and are never compared or retained; raw IDs, credentials, and runtime data are omitted. |
| `Simple/current-game-language-copy` characterization | x64 `1.152.11` | Two-lane characterization on 2026-09-16; repeatable candidate difference | After sign-in, each host calls public `IApps::GetCurrentGameLanguageCopy()` with default base-game ID into transient one-byte and 256-byte non-null buffers. | Not a CTest contract. Both official profiles twice returned `runtime-error` and left both sentinel-filled buffers unchanged; UniverseLAN reported no error and changed/terminated both buffers. The header does not specify termination, truncation, or these capacities, so this is a narrow candidate requiring another independently justified public form before classification. No language bytes, buffers, pointers, IDs, credentials, or runtime data are retained. |
| `Simple/current-game-language-code` characterization | x64 `1.152.11` | Two-lane characterization on 2026-09-16; repeatable candidate difference | After sign-in, each host calls public `IApps::GetCurrentGameLanguageCode()` once with its default base-game product ID. | Not a CTest contract. In two runs, both lanes exposed a non-null result matching the documented `ISO-639`-hyphen-`ISO-3166` shape; official reported `runtime-error` and UniverseLAN no error. Raw code text and pointer values are never retained. |
| `Simple/current-game-language-code-copy` characterization | x64 `1.152.11` | Two-lane characterization on 2026-09-16; repeatable candidate difference | After sign-in, each host calls public `IApps::GetCurrentGameLanguageCodeCopy()` with default base-game ID into transient one-byte and 256-byte non-null buffers. | Not a CTest contract. In two runs, official returned `runtime-error` and left both buffers unchanged; UniverseLAN reported no error, changed/terminated both buffers, and its bounded result had the documented code shape. Capacity, truncation, and termination are undocumented; no code text, buffer contents, pointers, IDs, credentials, or runtime data are retained. |
| `Simple/overlay-state` characterization | x64 `1.152.11` | Two-lane characterization on 2026-09-16; repeatable environment-dependent candidate difference | After sign-in, each host calls public `IUtils::GetOverlayState()` once. | Not a CTest contract. Two runs reported available `IUtils` and no error in both lanes; official returned documented `undefined`, UniverseLAN documented `disabled`. The SDK states overlay state depends on user enablement, support, and injection, so no equality contract or overlay action is inferred. |
| `Simple/storage-file-count` characterization | x64 `1.152.11` | Two-lane characterization on 2026-09-16; repeatable storage-state candidate difference | After sign-in and before any storage operation, each host calls public `IStorage::GetFileCount()` once. | Not a CTest contract. Two runs reported available storage and no error in both lanes; official had a nonzero-file relation while UniverseLAN was empty. Storage contents are title/environment state, so raw counts, names, paths, and contents are excluded and no equality contract is inferred. |
| `Simple/storage-file-exists` characterization | x64 `1.152.11` | Two-lane characterization on 2026-09-16; matched fresh-path relation | After sign-in, each host calls public `IStorage::FileExists()` once for a new in-memory generated portable relative path and performs no storage mutation. | Not a CTest contract. Two runs reported available storage, absent path, and no error in both lanes. The generated path is never retained; existing-file behavior requires an isolated fixture. See [`IStorage.h` header coverage](InterfaceBehaviourTestPlans/IStorage.h/HEADER_COVERAGE.md). |
| `IUtils.h` selected-header inventory | x64 `1.152.11` | Complete declaration inventory | `GetGogServicesConnectionState` and its listener have existing strict/diagnostic coverage; `GetOverlayState` is environment characterization. | Image, notification, and overlay APIs are fixture-, runtime-, or precondition-blocked; direct destruction is invalid. See [`IUtils.h` header coverage](InterfaceBehaviourTestPlans/IUtils.h/HEADER_COVERAGE.md). |
| `IStats.h` selected-header inventory | x64 `1.152.11` | Complete declaration inventory | Stats/achievement self retrieval and completion relation have existing coverage. | Leaderboard/time-played request forms are runtime-blocked; achievement/stat/leaderboard names and all state-changing methods require title fixtures or cleanup. See [`IStats.h` header coverage](InterfaceBehaviourTestPlans/IStats.h/HEADER_COVERAGE.md). |
| `IUser.h` selected-header inventory | x64 `1.152.11` | Complete declaration inventory | Sign-in, auth, self, logged-on, and session relations have existing coverage. | A key-free default-self user-data request is the only safe pending probe; token, ticket, user-data, external-authentication, and control APIs are bounded by sensitive-data, fixture, or mutation policy. See [`IUser.h` header coverage](InterfaceBehaviourTestPlans/IUser.h/HEADER_COVERAGE.md). |
| `IMatchmaking.h` selected-header inventory | x64 `1.152.11` | Complete practical public-lobby inventory | Controlled public-lobby scenarios cover creation, listing/filtering, joining/leaving, data, indexed access, messaging, ownership, and callbacks. | Direct-ID, arbitrary-data, and foreign-lobby forms remain fixture-blocked. See [`IMatchmaking.h` header coverage](InterfaceBehaviourTestPlans/IMatchmaking.h/HEADER_COVERAGE.md). |
| `INetworking.h` selected-header inventory | x64 `1.152.11` | Complete practical P2P inventory | Controlled scenarios cover reliable/unreliable send, availability, peek/read, packet callbacks, and self direct connection type. | NAT, immediate-send, arbitrary-peer/channel/payload, ping, and server-listener semantics remain fixture- or header-gated. See [`INetworking.h` header coverage](InterfaceBehaviourTestPlans/INetworking.h/HEADER_COVERAGE.md). |
| `IChat.h` selected-header inventory | x64 `1.152.11` | Complete practical chat inventory | Controlled room-message scenarios cover retrieval, send/read state, and callbacks without retaining content. | Room-with-user, membership, and inbound-message behavior require an approved friend/second-user fixture. See [`IChat.h` header coverage](InterfaceBehaviourTestPlans/IChat.h/HEADER_COVERAGE.md). |
| `IFriends.h` selected-header inventory | x64 `1.152.11` | Complete practical social inventory | Non-mutating peer-information coverage exists for request, availability, persona state/name, and callback relations. | Friendship, invitations, rich presence, game invites, and overlay actions require approved social, owned-key, or UI fixtures. See [`IFriends.h` header coverage](InterfaceBehaviourTestPlans/IFriends.h/HEADER_COVERAGE.md). |
| `ICustomNetworking.h` selected-header inventory | x64 `1.152.11` | Complete practical experimental-networking inventory | Official-only open-failure diagnostics cover supported and malformed URL forms. | Open-success, send/data, buffer access, and close remain gated on a successful official connection; runner echo behavior is not compatibility evidence. See [`ICustomNetworking.h` header coverage](InterfaceBehaviourTestPlans/ICustomNetworking.h/HEADER_COVERAGE.md). |
| `IListenerRegistrar.h` selected-header inventory | x64 `1.152.11` | Complete practical listener-lifecycle inventory | Global listener construction, callback delivery, and destruction before shutdown are exercised indirectly by controlled scenarios. | Direct registration, pending-operation unregister, and game-server aliases require a safe owned trigger or header-gated fixture. See [`IListenerRegistrar.h` header coverage](InterfaceBehaviourTestPlans/IListenerRegistrar.h/HEADER_COVERAGE.md). |
| `GalaxyGameServerApi.h` selected-header inventory | x64 `1.152.11` | Complete practical global-server inventory | No global game-server declaration has grounded controlled behavior coverage; the existing factory-local server-networking sender-identity probe remains separate. | Initialization, accessors, callbacks, lobby/P2P, and administration require owned server/client, credential, or policy-approved fixtures. See [`GalaxyGameServerApi.h` header coverage](InterfaceBehaviourTestPlans/GalaxyGameServerApi.h/HEADER_COVERAGE.md). |
| `Simple/public-lobby-create-list-join-leave` | x64 `1.152.11` | Implemented; indexed-accessor extension matched its first focused four-host run on 2026-09-16 | Public/joinable FCM lobby creation at capacity two; create/automatic-enter identity relation; tagged metadata update; callback-local filtered-list indexing; member/owner snapshots; in-range `GetLobbyMemberByIndex`; and indexed lobby/member-data reads before joiner-first then creator leave callbacks | The concurrent four-host runner compares scenario-specific role records per profile. It records no raw Galaxy IDs, indexes, keys, token, or member-data values: only validity, type, created/entered and listed/joined equality, self/other/owner/member relations, local-member-at-index-zero, repeated-read stability, symbolic indexed-data visibility, callback result/reason, public/joinable/capacity, and bounded-list retry facts. Causal gates, not global member-callback order, require creator-ready, joiner join, creator two-member snapshot, joiner leave, creator sole-owner snapshot, then creator leave. Every lane validates and retains its one-to-six-attempt convergence fact, but attempt count/retry timing is not cross-lane equality because the public list is eventually consistent. The observed official two-attempt versus UniverseLAN one-attempt convergence is an accepted beneficial variation. A second official characterization remains required before treating the indexed local-member order as stable. |
| `Simple/public-lobby-string-filtering` | x64 `1.152.11` | Implemented; official characterization evidence and focused strict live baseline on 2026-09-14 | User1 sequentially creates two initially nonjoinable public capacity-two FCM lobbies, configures distinct opaque marker-derived values under one fixed public filter key, then explicitly makes each joinable. User2 issues the target equality-filtered list request, obtains indexed candidates only callback-locally, privately classifies every listed candidate, selects only a predicate match, joins it, verifies two-member/non-self-owner state, and leaves. | List retry is a bounded eventual-visibility diagnostic. Both lanes strictly require target appearance, predicate match, correct selected join, membership, owner, and lifecycle cleanup. Stable official GOG requires no symbolic unmatched candidate. UniverseLAN intentionally accepts a public-candidate superset: symbolic unmatched-candidate presence/exclusion is retained as diagnostic only, while an unmatched candidate may never be selected or joined. This narrow policy does not accept malformed filters. Traces contain no raw IDs, lobby IDs, values, candidate counts, indexes, timestamps, controls, or runtime output. |
| `Simple/public-lobby-numerical-filtering` | x64 `1.152.11` | Passed two-lane comparison on 2026-09-16 (9.76s) | User1 sequentially creates two initially nonjoinable public capacity-two FCM lobbies, sets fixed distinct numerical properties and one shared token-derived run marker, then explicitly makes each joinable. User2 combines `AddRequestLobbyListNumericalFilter(..., LOBBY_COMPARISON_TYPE_EQUAL)` with the run-marker filter, privately classifies callback-local candidates, joins only the numerical target, verifies public two-member/non-self-owner state, and leaves. | Both lanes strictly require target appearance, numerical predicate match, absence of a run-owned numerical nonmatch, target-only selection/join, public two-member/non-self-owner state, and all leave cleanup. Bounded retry and the retained unmatched boolean are excluded from cross-lane equality; no product difference was observed. Traces never contain numeric or marker values, IDs, counts, indexes, timestamps, or runtime data. |
| `Simple/public-lobby-not-joinable-behavior` characterization | x64 `1.152.11` | Implemented; two official-GOG-only trials on 2026-09-14 | Creator creates public capacity-two FCM lobby nonjoinable, applies the public collision marker, explicitly calls `SetLobbyJoinable(false)`, and verifies public type/capacity/joinability/tag visibility. Joiner performs one marker-filtered non-full list request. | Both official trials returned successful zero-candidate lists, so no join was issued. The joiner makes no direct-ID join, member, member-data, or lobby-send call. Creator remains sole owner/member, terminally leaves with a fresh deadline, and the joiner observes a successful post-delete tagged-list absence. List visibility, join result, callback order, and failure reasons were not assumed before characterization. |
| `Simple/public-lobby-not-joinable-behavior` | x64 `1.152.11` | Implemented; matched live baseline on 2026-09-14 | Stable list-hidden branch of the characterization: public tagged capacity-two FCM lobby configured and visibly nonjoinable through `SetLobbyJoinable(false)`; one filtered list has no selected candidate; no join, member, member-data, or send access; creator sole ownership; terminal leave and deletion probe. | Exact same-profile records require successful list result, zero candidates, no selected relationship, no join record, no direct-ID/member/member-data/send attempt, creator sole owner/member, and successful post-delete absence. The contract deliberately does not claim a direct-ID join terminal reason, list callback ordering, or a different list-visibility branch because official GOG did not expose those facts. Both lanes acknowledge cleanup. |
| `Simple/public-lobby-full-join-failure` characterization | x64 `1.152.11` | Implemented; two official-GOG-only trials | Initially nonjoinable tagged public FCM lobby with an explicit single-member limit; joiner filtered full-lobby list, public lobby-data retrieval/state, one join attempt, and post-empty list | Official-only and not CTest. Both trials observed terminal `full`, no entry, creator sole owner/member, and post-empty tagged-list absence. Ordered records are per-host `IMatchmaking` observations; creator-ready, join terminal, creator leave, and post-empty probe are causal gates only. Raw IDs, marker/configuration values, and timestamps are omitted. |
| `Simple/public-lobby-full-join-failure` | x64 `1.152.11` | Implemented; matched live baseline | The stable official facts plus creator terminal leave and joiner nonmember completion | Exact same-profile records require the asynchronous `full` terminal result, exactly one terminal callback, no entry, creator owner/sole-member state after the joiner failure, and post-empty tagged-list absence. The joiner calls only full-list, lobby-data retrieval, and public state APIs; member-data, member enumeration, and lobby-message calls are absent by contract. Creator cleanup uses a fresh deadline and is acknowledged only after its local terminal leave; the joiner has no leave acknowledgement because it never became a member. |
| `Simple/public-lobby-owner-close-lifecycle` characterization | x64 `1.152.11` | Implemented; two official-GOG-only characterizations | Creator normal `LeaveLobby`; joiner public global member-state and lobby-left listeners armed before the causal release; post-close filtered list | `LeaveLobby` generically documents member notifications, while FCM documentation discusses owner disconnection rather than normal leave. Both official runs empirically observed a joiner `lobby-closed` callback and tagged-list absence with no targeted prior-owner member-state callback; that narrow absence is not a header guarantee. Target-lobby sequences are retained diagnostically with no cross-listener ordering assertion. Successful diagnostic roots are sanitized; controls, configuration, relays, and runtime output are removed. |
| `Simple/public-lobby-owner-close-lifecycle` | x64 `1.152.11` | Implemented; matched live baseline | The stable official characterization facts plus all existing public create/configuration/list/join relations | Exact same-profile records compare creator local leave, listener registration, empirical prior-owner callback absence, close/list-absence, and all setup facts. The probe finalizes target presence/absence after post-close listing and a bounded final settle window. List retry and target-lobby sequence order/multiplicity are diagnostic only, but any target global leave reason other than `lobby-closed` fails. Creator cleanup acknowledgement requires a matching terminal leave callback; a bounded retry may acknowledge only when public owner/member state proves retry safety and its terminal callback confirms cleanup. |
| `Simple/public-lobby-owner-ownership-transition` characterization | x64 `1.152.11` | Implemented; two official-GOG-only trials on 2026-09-14 | Public ownership-transition topology, creator/joiner local leaves, joiner global member-state and owner-change listeners, owner/cache promotion, promoted-owner data authorization, and post-empty filtered list | Official-only and not CTest. Both trials observed prior-owner `left`, owner-change-to-self, self as the sole member and owner, successful token-derived promoted-owner data update with copied visibility, and tagged-list absence after joiner leave. Global-listener ordering and list retries are retained but not promoted. Successful roots are sanitized. |
| `Simple/public-lobby-owner-ownership-transition` | x64 `1.152.11` | Implemented; focused strict four-host baseline matched on 2026-09-14 | Tagged public `LOBBY_TOPOLOGY_TYPE_FCM_OWNERSHIP_TRANSITION` creation; explicit joinable/capacity-two setup; filtered join; initial two-member prior-owner snapshot; member/owner transition; promoted-owner `SetLobbyData`; both terminal leaves; post-empty list | Exact same-profile records require all stable public relations: creator and joiner `user-left`, prior-owner `LEFT`, owner-change-to-self, owner-only cache, promoted-owner operation/copy success, and token-filtered absence. Member/owner listener sequence and callback multiplicity have no cross-class ordering rule; their symbolic records are diagnostic. Bounded list retries are also diagnostic. The former owner stays alive until the post-empty probe, and both acknowledgements require confirmed local terminal leaves. |
| `Simple/public-lobby-data-propagation` | x64 `1.152.11` | Implemented; characterized and recorded as a matched live baseline | Post-join `GlobalLobbyDataListener` arming, owner `SetLobbyData` on a separate fixed key with a nonempty runner collision-marker-derived value, and joiner `GetLobbyDataCopy` during notification and after bounded pumping | Ordered role records are exact within each host's logical `IMatchmaking` sequence. Both lanes require the existing create/list/join relations, owner data-update success, a joined-lobby callback, invalid symbolic callback `memberID` for lobby-level data, and same-callback plus eventual copied-value visibility. Joiner cleanup precedes creator cleanup. Bounded list convergence, callback count/duplicates, and callback-gate discovery order are diagnostic only, never cross-host total-order assertions. No marker, key, value, raw ID, persona, credential, or timestamp is recorded. |
| `Simple/chat-room-message-delivery` characterization | x64 `1.152.11` | Implemented, explicit runner diagnostic | Public `IChat` room-request terminal result, send terminal relation, receiver callback-local message read, sender/type/token relations, and ordered records per host | Not a CTest contract. It uses the full concurrent four-host runner and retains only symbolic reports. Receiver listener registration causally precedes sender release; sender terminal and receiver message callbacks have no cross-host total order. |
| `Simple/chat-room-message-delivery` | x64 `1.152.11` | Implemented; 2026-09-14 matched live baseline | Public `IChat::RequestChatRoomWithUser`, `SendChatRoomMessage`, operation listeners, and `GlobalChatRoomMessagesListener` callback-local read | Exact role-local ordered `IChat` records require one successful room terminal with symbolic peer/room relation, one successful send terminal tied to symbolic room/index, and one callback-local receive with matching symbolic sender, chat type, and private-token equality. One-time raw-ID/token relays are consumed and removed. Host exit is the agreed cleanup assumption; no unavailable delete API is claimed. The official run requires the approved profiles to be friends with messaging privacy permitting friends; this is an external test-environment precondition, not a product-cause claim. |
| `Simple/bidirectional-chat-room-message-delivery` characterization | x64 `1.152.11` | Implemented; two clean official-GOG-only trials on 2026-09-14 | Both signed-in profiles register public `GlobalChatRoomMessagesListener` before readiness; user1 initiates and user2 causally resolves the same one-to-one `IChat` room; each validates public two-member self/peer membership, sends once after the shared-room gate, and reads only callback-locally. | Official-only and not CTest. Both trials completed one successful request and one successful send per host, with one matching self and one matching other chat-message relation per host. Self echoes were present, but callback batching/cardinality and self/other order varied between trials, so they remain same-host diagnostics only. No cross-host/global order is asserted. The private run root, controls, relays, runtime output, and all artifacts are removed after the characterization. |
| `Simple/bidirectional-chat-room-message-delivery` | x64 `1.152.11` | Implemented; focused strict and full 16-test behavior suite pass on 2026-09-14 | The characterized public `IChat` setup plus distinct token-derived one-send-per-direction payloads, callback-local reads, symbolic shared-room/self-other sender/chat-type/payload-size relations, and bounded host exit. | Exact same-profile records require listener arming before either request, user1 initiation before user2 resolution, local one-to-one room membership, one successful send terminal per direction, and exactly one validated remote directional message per host. Self echo count, callback cardinality/batching, self/other local order, and token-mismatched prior-room messages are retained but excluded from equality. Prior messages are read only inside their callback and never contribute to the fresh private-token relation. No raw room deletion is claimed; all hosts must exit and all relay/control/token artifacts are removed or redacted. |
| `Simple/friends-peer-information-retrieval` characterization | x64 `1.152.11` | Implemented; three clean official-only trials plus one later official variation on 2026-09-14 | Public `GlobalPersonaDataChangedListener`, `RequestUserInformation(peer, AVATAR_TYPE_NONE, listener)`, `IsUserInformationAvailable`, `GetFriendPersonaNameCopy`, and `GetFriendPersonaState` | Official-only and not CTest. Both signed-in hosts register the global listener before the runner consumes one self-ID relay and emits one peer-ID relay. The listener filters to the requested peer. Three clean trials observed ordered `name-avatar`, then `none`; a later full run observed only terminal `none` for one official profile. The sequence remains retained requested-peer diagnostic context, not equality-required. `AVATAR_TYPE_NONE` does not assert absent avatar activity because default avatar criteria can apply. Successful roots are removed. |
| `Simple/friends-peer-information-retrieval` | x64 `1.152.11` | Implemented; accepted beneficial difference on 2026-09-14 | The characterization facts, callback peer=requested-peer and valid non-self relation, terminal outcome, post-terminal availability, copied-name nonemptiness, and symbolic friend persona state | Exact ordered role-local `IFriends` records per host require terminal `success`, callback-peer/requested-peer and valid-non-self relations, available information, and a nonempty copied name. The manifest declares the only accepted difference: observed `GOG offline` versus `UniverseLAN online` persona state, consistent with the service-state environment where official GOG has no GOG Galaxy service while UniverseLAN provides its LAN service. It is an environmental observation, not an SDK guarantee; any other state or record mismatch fails. Requested-peer persona events are retained but excluded from equality after a later official `none`-only variation; arbitrary-user events are filtered out. No raw ID, persona name, avatar data/URL/image ID, count, status text, timestamp, credential, runtime output, control, or relay is recorded. Retrieval has no friendship or direct-message privacy precondition. |
| `Simple/reliable-p2p-listener-peek` characterization | x64 `1.152.11` | Implemented; three clean official-GOG-only trials on 2026-09-14 | Creator creates a public, initially nonjoinable, capacity-two FCM lobby, sets its public collision marker, explicitly observes joinable before discovery, completes a bounded post-arm settling window, and sends one nonempty bounded opaque binary payload with `P2P_SEND_RELIABLE` on the fixed channel. Joiner constructs `GlobalNetworkingListener` before creator release and makes two `PeekP2PPacket` calls for each expected-channel networking callback, using callback-sized private buffers. | The header says `SendP2PPacket` returns scheduling state, recommends bounded packets, and specifies that peek is non-consuming; it directs listener users to peek during notification rather than poll, read, or pop. It does not guarantee delivery from a true send result. All three official trials completed setup and scheduled send, then observed one non-target callback with no expected-channel callback or successful peek pair. Both leaves were terminally confirmed. No raw ID, lobby ID, payload bytes, marker value, or message length is retained. |
| `Simple/reliable-p2p-listener-peek` | x64 `1.152.11` | Implemented; strict exact-delivery contract, unresolved with verified channel-8 overlay | The characterization facts plus symbolic recipient current-membership/type, callback channel/size relation, callback-local non-consuming peeks, valid non-self creator sender relation, opaque payload/length relation, and listener destruction before shutdown. | Exact same-profile records require one expected-channel callback and two equivalent successful peeks in both lanes. No no-delivery pair is accepted. The verified 2026-09-14 overlay run staged the expected peer hash but still failed this contract, so it is an official runtime configuration mismatch rather than a product difference. |
| `Simple/reliable-p2p-after-lobby-leave` characterization | x64 `1.152.11` | Implemented; two official-GOG-only trials on 2026-09-14 | Public nonjoinable-to-joinable tagged capacity-two FCM lobby; both public `GlobalLobbyMemberStateListener` and `GlobalNetworkingListener` armed before user2 confirms `LeaveLobby`; user1 then settles public one-member ownership and sends one reliable opaque payload to the former valid user ID while user2 remains alive. | Both trials observed the targeted member `left` callback, settled sole-owner membership, and `SendP2PPacket` scheduling `true`. The former member received no networking callback on any channel and made no peek. This is an empirical post-leave result, not a `SendP2PPacket` delivery guarantee. Listener-mode code never polls, reads, or pops; IDs, tokens, marker values, bytes, and lengths are absent from retained reports. |
| `Simple/reliable-p2p-after-lobby-leave` | x64 `1.152.11` | Implemented; focused matched live baseline on 2026-09-14 | The stable official post-leave setup: user2's confirmed local `user-left`; user1's targeted public member-state `left`; one-member/self-owner settlement; and one reliable send to the former valid user ID while the former member's networking listener remains alive. | Exact same-profile records require the public leave/member/membership/scheduling relations, zero callback and peek relations on the former member, listener destruction, and fresh-deadline creator cleanup. The focused comparison matched directly: UniverseLAN exposes no post-leave listener callback or peek, so no LAN benefit or accepted difference is claimed. Callback multiplicity/order is retained only insofar as zero delivery is the characterized exact result. |
| `Simple/bidirectional-reliable-p2p-listener-peek` characterization | x64 `1.152.11` | Implemented; three clean official-GOG-only trials on 2026-09-14 | One public, initially nonjoinable, capacity-two FCM lobby per lane with public collision marker/configuration and explicit observed joinability before filtered list/join. Both members publicly query the other lobby member, construct `GlobalNetworkingListener`, complete the post-arm settling window, and receive the shared release before one distinct `P2P_SEND_RELIABLE` payload per direction on fixed non-default channels. | Official-only and not CTest. Each expected-channel callback performs exactly two callback-local non-consuming peeks only; listener-mode code never polls, reads, or pops. All three trials scheduled both sends, then observed one non-target callback, no expected-channel callback, and zero peeks in each direction. Both listeners were destroyed before shutdown and all terminal fresh-deadline leaves were acknowledged. No raw ID, lobby ID, marker, token, payload byte, or length is retained. |
| `Simple/bidirectional-reliable-p2p-listener-peek` | x64 `1.152.11` | Implemented; strict exact-delivery contract, unresolved with verified channel-8 overlay | Stable official setup plus symbolic public-peer/current-membership, shared-lobby, distinct-direction channel/payload, listener settling, synchronized release, scheduling, callback-local peek, destruction, and cleanup relations for `user1` to `user2` and independently for `user2` to `user1`. | Every setup, peer/lobby/channel/payload, listener lifecycle, delivery, and cleanup relation is strict. Each direction requires one expected-channel callback and two equivalent successful peeks in both lanes; no no-delivery pair is accepted. |
| `Simple/bidirectional-reliable-p2p-poll-read` characterization | x64 `1.152.11` | Implemented; three clean official-GOG-only runs on 2026-09-14 | Two signed-in official hosts create and join a temporary public capacity-two FCM lobby, derive the peer only from public lobby membership, synchronize on both polling paths being armed, then each schedules one reliable directional packet on a fixed non-default channel. No `INetworkingListener` is constructed. Each `ProcessData` polling iteration calls `IsP2PPacketAvailable`, then, only when available, performs one exact-size `ReadP2PPacket` and immediately checks expected-channel availability again. | Official-only and not CTest. All three trials observed availability, one successful read, matching symbolic peer/payload relations, no listener, empty expected-channel availability immediately after read, and terminal cleanup in both directions. Records retain only symbolic relations; raw IDs, lobby IDs, marker values, payload bytes/lengths, tokens, controls, timestamps, credentials, runtime output, and artifact paths are omitted. |
| `Simple/bidirectional-reliable-p2p-poll-read` | x64 `1.152.11` | Implemented; focused strict four-host baseline and self connection-type extension matched on 2026-09-16 | The stable listener-free polling setup, separate reliable direction channels and payloads, one exact-size `ReadP2PPacket` only after expected-channel availability, public `GetConnectionType(self)`, symbolic sender/payload relations, immediate post-read empty expected-channel availability, and terminal cleanup. | Exact same-profile records require public peer/current-membership, documented self `CONNECTION_TYPE_DIRECT`, distinct channels/payloads, one scheduled reliable send per host, no listener construction, availability after each polling `ProcessData`, one read only when available, read success, symbolic expected sender/payload/size, post-read availability check, and empty queue relation. The current peer connection enum is retained as public diagnostic context and excluded from cross-lane equality because the header documents only the self-direct relation. Listener-mode P2P contracts remain separate: they use callback-local non-consuming peek only and retain their accepted no-delivery diagnostics. |
| `Simple/bidirectional-unreliable-p2p-listener-peek` characterization | x64 `1.152.11` | Implemented; three clean official-GOG-only trials on 2026-09-14 | One public, initially nonjoinable, capacity-two FCM lobby per lane with public collision marker/configuration and observed joinability before filtered list/join. Both members publicly query the other current lobby member, arm and settle `GlobalNetworkingListener`, then receive the shared release before one distinct `P2P_SEND_UNRELIABLE` payload per direction on fixed non-default channels. | Official-only and not CTest. Each expected-channel callback performs exactly two callback-local non-consuming peeks only; listener-mode code never polls, reads, or pops. All three trials scheduled both sends, then observed one non-target callback, no expected-channel callback, and zero peeks in each direction. The UDP-like send type and a true `SendP2PPacket` result do not guarantee delivery. Both listeners were destroyed before shutdown and all terminal fresh-deadline leaves were acknowledged. No raw ID, lobby ID, marker, token, payload byte, or length is retained. |
| `Simple/bidirectional-unreliable-p2p-listener-peek` | x64 `1.152.11` | Implemented; strict exact-delivery contract, unresolved with verified channel-8 overlay | Stable official setup plus symbolic public-peer/current-membership, distinct-direction channel/payload, listener settling, synchronized release, `P2P_SEND_UNRELIABLE` scheduling, callback-local peek, destruction, and cleanup relations for each direction. | Every setup, peer/lobby/channel/payload, listener lifecycle, delivery, and cleanup relation is strict. Each direction requires one expected-channel callback and two equivalent successful peeks in both lanes; no no-delivery pair is accepted. A true UDP-like send result still reports scheduling rather than a general delivery guarantee. |
| `Simple/bidirectional-lobby-message-delivery` characterization | x64 `1.152.11` | Implemented; two official-GOG-only trials on 2026-09-14 | One initially nonjoinable tagged public/capacity-two FCM lobby; explicit marker/capacity updates and observed `SetLobbyJoinable(true)` before discovery; both public `GlobalLobbyMessageListener` instances armed before exchange; one distinct opaque token-derived binary send from each member; callback-local `GetLobbyMessage` only; and fresh-deadline terminal leaves. | Official-only and not CTest. Both trials observed a true send-scheduling result, exactly two callback-local reads per host, one symbolic self sender and one other sender, matching shared-lobby/payload/size relations, and local `self, other` callback order. Cardinality and order remain retained diagnostic evidence; no cross-host or independent-sender global order is asserted. Sanitized artifacts retain no marker, ID, payload, length, timestamp, control, log, or runtime output. |
| `Simple/bidirectional-lobby-message-delivery` | x64 `1.152.11` | Implemented; matched focused live baseline on 2026-09-14 | The stable official setup, listener, dual-send, callback-local read, symbolic sender, directional payload, size, scheduling, and terminal-cleanup relations. | Exact same-profile records require each host's exactly one scheduled send and exactly two callbacks: one self and one other, both from the shared symbolic lobby with callback-local read sender, payload, and message-size relations valid. The callback order is retained but excluded from equality because both sends are independent after the shared gate, so no global sender order is an SDK contract. The focused run observed official `self, other` for both roles; UniverseLAN matched creator order and observed joiner `other, self` while matching every strict relation and cleanup acknowledgement. |
| `Simple/bidirectional-lobby-member-data-propagation` characterization | x64 `1.152.11` | Implemented; two official-GOG-only trials on 2026-09-14 | One initially nonjoinable tagged public/capacity-two FCM lobby, explicit observed joinability before discovery, joined public two-member state, both armed `GlobalLobbyDataListener` instances, and one `SetLobbyMemberData` operation per member after a shared release. | Official-only and not CTest. The selected header declares the setter `void`; each operation listener supplied the terminal result. Both trials observed successful symbolic callback targets for self and other, callback-local changed-data visibility/private equality, and settled self/other nonempty equal copies. Callback count and local self/other order are retained only as diagnostics; no cross-host or sender order is asserted. |
| `Simple/bidirectional-lobby-member-data-propagation` | x64 `1.152.11` | Implemented; matched focused live baseline on 2026-09-14 | The characterized safe temporary-lobby lifecycle plus distinct fixed public member-data keys, opaque token-derived values, operation terminals, callback-local `GetLobbyMemberDataCopy`, and settled public copies for both directions. | Exact same-profile records require public configuration/discovery/membership, listener arming before shared release, one void-form setter invocation and successful terminal callback targeting the local member, symbolic shared-lobby self/other listener targets, changed-data visibility, private equality/nonempty, eventual self/other copies, and terminal cleanup. Callback count and local target order are retained per host but excluded from equality because updates are independent after the shared gate. No raw ID, key, marker, token, value, timestamp, control, or runtime output is retained. |
| `Advanced/multiple-lobby-membership-and-message-isolation` characterization | x64 `1.152.11` | Implemented; official-GOG-only characterization succeeded on 2026-09-14 | Two adjacent `CreateLobby` calls with operation-local listeners, automatic enter identity, explicit public/joinable/capacity-two setters and queries, tagged discovery, two adjacent `JoinLobby` calls, and callback-local message reads for symbolic `L0`/`L1` | Not CTest and not a lane comparison. Preserves each host's observed `IMatchmaking` callback order, does not impose cross-host order, and retains only a sanitized official report. The header exposes no lobby-message type, so that unavailable public field is recorded rather than asserted. |
| `Advanced/multiple-lobby-membership-and-message-isolation` | x64 `1.152.11` | Implemented; 2026-09-14 matched live baseline after official order characterization | The characterization facts plus creator’s two back-to-back opaque token-derived sends and both hosts’ callback-local sender/payload/symbolic-lobby isolation checks | Exact same-profile lane comparison preserves ordered callback records. The recorded joiner `L0`/`L1` cross-lobby message sequence is diagnostic only because two official runs observed opposite orders; it is retained, never sorted, and explicitly reported. This does not weaken create/join issuance or assert cross-host order. Each lane must explicitly observe public, joinable, capacity-two lobbies before join; joiner leaves `L1` then `L0`, followed by creator `L1` then `L0`, within the bounded deadline. No raw IDs, tags, payloads, message IDs, timestamps, or metadata values are recorded. |
| `Simple/custom-networking-loopback-roundtrip-close` characterization | x64 `1.152.11` | Implemented; two official-GOG-only trials on 2026-09-14, unresolved | Two signed-in official hosts independently call public `ICustomNetworking::OpenConnection` against a runner-owned ephemeral `ws://127.0.0.1` binary echo endpoint. On open success, the probe would send one private NUL-containing payload, validate callback-local availability, two non-consuming peeks, one read, and a close callback. | Both trials produced an open terminal failure for both profiles, so send, receive, and close were not issued. The endpoint only returns opaque binary frames and is not behavior evidence. This is stable public evidence for the local endpoint configuration only, not an SDK guarantee or a UniverseLAN candidate; no strict CTest is registered until successful official open/roundtrip/close observations are repeatable. IDs, URL/port, payload bytes/lengths, credentials, timestamps, controls, and runtime output are omitted. |

No social mutation, cloud, or telemetry scenario is implemented. The peer-information scenario only reads existing approved friend/profile data and never creates friendships or mutates social data. The temporary-lobby matchmaking scenarios create and leave tagged temporary lobbies in every lane. The authorized temporary chat scenario relies on bounded participant exit because the public SDK provides no delete-room API.
