# BehaviourTests Coverage

Read [`README.md`](README.md), [`BEHAVIOUR_TEST_PLAN.md`](BEHAVIOUR_TEST_PLAN.md),
and [`DEVELOPER_GUIDE.md`](DEVELOPER_GUIDE.md) before expanding this register.
Current milestones and future work are in [`TODO_PROGRESS.md`](TODO_PROGRESS.md).

| Scenario | SDK | State | Public observations | Comparison rule |
| --- | --- | --- | --- | --- |
| `Simple/initialize-and-sign-in` | x64 `1.152.11` | Implemented; live official run remains opt-in | Init return, auth terminal callback/outcome, `SignedIn`, `IsLoggedOn`, self ID validity/type/equality, and persona availability | Exact ordered four-record contract per lane; opaque IDs are lane-local symbols only; account-specific persona text is intentionally not captured; every other record fails. |
| `Simple/session-id-repeatability` | x64 `1.152.11` | Implemented; live official run remains opt-in | After successful `IAuthListener::OnAuthSuccess` and before another `ProcessData`, two consecutive public `IUser::GetSessionID()` calls | Exact ordered five-record contract per lane. The strict `session-id-repeatability` record contains only `equal`; successful sign-in and same-profile UniverseLAN/official boolean equality are required, with no source or manifest assumption that the relation is `true` or `false`. Raw session IDs, zero/nonzero classification, user IDs, persona text, credentials, timestamps, and internals are never recorded. |
| `Simple/gog-services-state` | x64 `1.152.11` | Implemented, accepted beneficial difference | The same sign-in path and self-state observations, then public `IUtils::GetGogServicesConnectionState()` immediately after auth success | Exact ordered five-record contract per same-profile lane pair, except the manifest-declared pair `GOG undefined` and `UniverseLAN connected`, which is classified as `accepted-difference`. It is accepted because the official lane has no GOG Galaxy service while UniverseLAN provides its LAN service. Any other state mismatch or any other record mismatch fails. No service-state listener or order rule is registered. |
| `Simple/gog-services-state` characterization | x64 `1.152.11` | Implemented, explicit runner diagnostic only | The same topology and sign-in path, with immediate, one-`ProcessData`, and ten-pump settled `IUtils::GetGogServicesConnectionState()` queries and symbolic `GlobalGogServicesConnectionStateListener` events phased as `before`, `immediate`, or `settled` | Not a CTest contract and not a comparator: no UniverseLAN/GOG equality or total callback-order assertion. Successful runs retain raw and normalized lane reports; no raw IDs, credentials, or timestamps are recorded. |
| `Simple/public-lobby-create-list-join-leave` | x64 `1.152.11` | Implemented; 2026-09-13 matched live baseline with accepted convergence difference | Public/joinable FCM lobby creation at capacity two; create/automatic-enter identity relation; tagged metadata update; filtered public list, selected-list-to-join identity relation, member/owner snapshots, and joiner-first then creator leave callbacks | The concurrent four-host runner compares scenario-specific role records per profile. It records no raw Galaxy IDs or token: only validity, type, created/entered and listed/joined equality, self/other/owner/member relations, symbolic callback result/reason, public/joinable/capacity, and bounded-list retry facts. Causal gates, not global member-callback order, require creator-ready, joiner join, creator two-member snapshot, joiner leave, creator sole-owner snapshot, then creator leave. Every lane validates and retains its one-to-six-attempt convergence fact, but attempt count/retry timing is not cross-lane equality because the public list is eventually consistent. The observed official two-attempt versus UniverseLAN one-attempt convergence is an accepted beneficial variation; all four hosts acknowledged cleanup. |
| `Simple/public-lobby-not-joinable-behavior` characterization | x64 `1.152.11` | Implemented; two official-GOG-only trials on 2026-09-14 | Creator creates public capacity-two FCM lobby nonjoinable, applies the public collision marker, explicitly calls `SetLobbyJoinable(false)`, and verifies public type/capacity/joinability/tag visibility. Joiner performs one marker-filtered non-full list request. | Both official trials returned successful zero-candidate lists, so no join was issued. The joiner makes no direct-ID join, member, member-data, or lobby-send call. Creator remains sole owner/member, terminally leaves with a fresh deadline, and the joiner observes a successful post-delete tagged-list absence. List visibility, join result, callback order, and failure reasons were not assumed before characterization. |
| `Simple/public-lobby-not-joinable-behavior` | x64 `1.152.11` | Implemented; matched live baseline on 2026-09-14 | Stable list-hidden branch of the characterization: public tagged capacity-two FCM lobby configured and visibly nonjoinable through `SetLobbyJoinable(false)`; one filtered list has no selected candidate; no join, member, member-data, or send access; creator sole ownership; terminal leave and deletion probe. | Exact same-profile records require successful list result, zero candidates, no selected relationship, no join record, no direct-ID/member/member-data/send attempt, creator sole owner/member, and successful post-delete absence. The contract deliberately does not claim a direct-ID join terminal reason, list callback ordering, or a different list-visibility branch because official GOG did not expose those facts. Both lanes acknowledge cleanup. |
| `Simple/public-lobby-full-join-failure` characterization | x64 `1.152.11` | Implemented; two official-GOG-only trials | Initially nonjoinable tagged public FCM lobby with an explicit single-member limit; joiner filtered full-lobby list, public lobby-data retrieval/state, one join attempt, and post-empty list | Official-only and not CTest. Both trials observed terminal `full`, no entry, creator sole owner/member, and post-empty tagged-list absence. Ordered records are per-host `IMatchmaking` observations; creator-ready, join terminal, creator leave, and post-empty probe are causal gates only. Raw IDs, marker/configuration values, and timestamps are omitted. |
| `Simple/public-lobby-full-join-failure` | x64 `1.152.11` | Implemented; matched live baseline | The stable official facts plus creator terminal leave and joiner nonmember completion | Exact same-profile records require the asynchronous `full` terminal result, exactly one terminal callback, no entry, creator owner/sole-member state after the joiner failure, and post-empty tagged-list absence. The joiner calls only full-list, lobby-data retrieval, and public state APIs; member-data, member enumeration, and lobby-message calls are absent by contract. Creator cleanup uses a fresh deadline and is acknowledged only after its local terminal leave; the joiner has no leave acknowledgement because it never became a member. |
| `Simple/public-lobby-owner-close-lifecycle` characterization | x64 `1.152.11` | Implemented; two official-GOG-only characterizations | Creator normal `LeaveLobby`; joiner public global member-state and lobby-left listeners armed before the causal release; post-close filtered list | `LeaveLobby` generically documents member notifications, while FCM documentation discusses owner disconnection rather than normal leave. Both official runs empirically observed a joiner `lobby-closed` callback and tagged-list absence with no targeted prior-owner member-state callback; that narrow absence is not a header guarantee. Target-lobby sequences are retained diagnostically with no cross-listener ordering assertion. Successful diagnostic roots are sanitized; controls, configuration, relays, and runtime output are removed. |
| `Simple/public-lobby-owner-close-lifecycle` | x64 `1.152.11` | Implemented; matched live baseline | The stable official characterization facts plus all existing public create/configuration/list/join relations | Exact same-profile records compare creator local leave, listener registration, empirical prior-owner callback absence, close/list-absence, and all setup facts. The probe finalizes target presence/absence after post-close listing and a bounded final settle window. List retry and target-lobby sequence order/multiplicity are diagnostic only, but any target global leave reason other than `lobby-closed` fails. Creator cleanup acknowledgement requires a matching terminal leave callback; a bounded retry may acknowledge only when public owner/member state proves retry safety and its terminal callback confirms cleanup. |
| `Simple/public-lobby-owner-ownership-transition` characterization | x64 `1.152.11` | Implemented; two official-GOG-only trials on 2026-09-14 | Public ownership-transition topology, creator/joiner local leaves, joiner global member-state and owner-change listeners, owner/cache promotion, promoted-owner data authorization, and post-empty filtered list | Official-only and not CTest. Both trials observed prior-owner `left`, owner-change-to-self, self as the sole member and owner, successful token-derived promoted-owner data update with copied visibility, and tagged-list absence after joiner leave. Global-listener ordering and list retries are retained but not promoted. Successful roots are sanitized. |
| `Simple/public-lobby-owner-ownership-transition` | x64 `1.152.11` | Implemented; focused strict four-host baseline matched on 2026-09-14 | Tagged public `LOBBY_TOPOLOGY_TYPE_FCM_OWNERSHIP_TRANSITION` creation; explicit joinable/capacity-two setup; filtered join; initial two-member prior-owner snapshot; member/owner transition; promoted-owner `SetLobbyData`; both terminal leaves; post-empty list | Exact same-profile records require all stable public relations: creator and joiner `user-left`, prior-owner `LEFT`, owner-change-to-self, owner-only cache, promoted-owner operation/copy success, and token-filtered absence. Member/owner listener sequence and callback multiplicity have no cross-class ordering rule; their symbolic records are diagnostic. Bounded list retries are also diagnostic. The former owner stays alive until the post-empty probe, and both acknowledgements require confirmed local terminal leaves. |
| `Simple/public-lobby-data-propagation` | x64 `1.152.11` | Implemented; characterized and recorded as a matched live baseline | Post-join `GlobalLobbyDataListener` arming, owner `SetLobbyData` on a separate fixed key with a nonempty runner collision-marker-derived value, and joiner `GetLobbyDataCopy` during notification and after bounded pumping | Ordered role records are exact within each host's logical `IMatchmaking` sequence. Both lanes require the existing create/list/join relations, owner data-update success, a joined-lobby callback, invalid symbolic callback `memberID` for lobby-level data, and same-callback plus eventual copied-value visibility. Joiner cleanup precedes creator cleanup. Bounded list convergence, callback count/duplicates, and callback-gate discovery order are diagnostic only, never cross-host total-order assertions. No marker, key, value, raw ID, persona, credential, or timestamp is recorded. |
| `Simple/chat-room-message-delivery` characterization | x64 `1.152.11` | Implemented, explicit runner diagnostic | Public `IChat` room-request terminal result, send terminal relation, receiver callback-local message read, sender/type/token relations, and ordered records per host | Not a CTest contract. It uses the full concurrent four-host runner and retains only symbolic reports. Receiver listener registration causally precedes sender release; sender terminal and receiver message callbacks have no cross-host total order. |
| `Simple/chat-room-message-delivery` | x64 `1.152.11` | Implemented; 2026-09-14 matched live baseline | Public `IChat::RequestChatRoomWithUser`, `SendChatRoomMessage`, operation listeners, and `GlobalChatRoomMessagesListener` callback-local read | Exact role-local ordered `IChat` records require one successful room terminal with symbolic peer/room relation, one successful send terminal tied to symbolic room/index, and one callback-local receive with matching symbolic sender, chat type, and private-token equality. One-time raw-ID/token relays are consumed and removed. Host exit is the agreed cleanup assumption; no unavailable delete API is claimed. The official run requires the approved profiles to be friends with messaging privacy permitting friends; this is an external test-environment precondition, not a product-cause claim. |
| `Simple/bidirectional-chat-room-message-delivery` characterization | x64 `1.152.11` | Implemented; two clean official-GOG-only trials on 2026-09-14 | Both signed-in profiles register public `GlobalChatRoomMessagesListener` before readiness; user1 initiates and user2 causally resolves the same one-to-one `IChat` room; each validates public two-member self/peer membership, sends once after the shared-room gate, and reads only callback-locally. | Official-only and not CTest. Both trials completed one successful request and one successful send per host, with one matching self and one matching other chat-message relation per host. Self echoes were present, but callback batching/cardinality and self/other order varied between trials, so they remain same-host diagnostics only. No cross-host/global order is asserted. The private run root, controls, relays, runtime output, and all artifacts are removed after the characterization. |
| `Simple/bidirectional-chat-room-message-delivery` | x64 `1.152.11` | Implemented; focused strict and full 16-test behavior suite pass on 2026-09-14 | The characterized public `IChat` setup plus distinct token-derived one-send-per-direction payloads, callback-local reads, symbolic shared-room/self-other sender/chat-type/payload-size relations, and bounded host exit. | Exact same-profile records require listener arming before either request, user1 initiation before user2 resolution, local one-to-one room membership, one successful send terminal per direction, and exactly one validated remote directional message per host. Self echo count, callback cardinality/batching, self/other local order, and token-mismatched prior-room messages are retained but excluded from equality. Prior messages are read only inside their callback and never contribute to the fresh private-token relation. No raw room deletion is claimed; all hosts must exit and all relay/control/token artifacts are removed or redacted. |
| `Simple/friends-peer-information-retrieval` characterization | x64 `1.152.11` | Implemented; three clean official-only trials plus one later official variation on 2026-09-14 | Public `GlobalPersonaDataChangedListener`, `RequestUserInformation(peer, AVATAR_TYPE_NONE, listener)`, `IsUserInformationAvailable`, `GetFriendPersonaNameCopy`, and `GetFriendPersonaState` | Official-only and not CTest. Both signed-in hosts register the global listener before the runner consumes one self-ID relay and emits one peer-ID relay. The listener filters to the requested peer. Three clean trials observed ordered `name-avatar`, then `none`; a later full run observed only terminal `none` for one official profile. The sequence remains retained requested-peer diagnostic context, not equality-required. `AVATAR_TYPE_NONE` does not assert absent avatar activity because default avatar criteria can apply. Successful roots are removed. |
| `Simple/friends-peer-information-retrieval` | x64 `1.152.11` | Implemented; accepted beneficial difference on 2026-09-14 | The characterization facts, callback peer=requested-peer and valid non-self relation, terminal outcome, post-terminal availability, copied-name nonemptiness, and symbolic friend persona state | Exact ordered role-local `IFriends` records per host require terminal `success`, callback-peer/requested-peer and valid-non-self relations, available information, and a nonempty copied name. The manifest declares the only accepted difference: observed `GOG offline` versus `UniverseLAN online` persona state, consistent with the service-state environment where official GOG has no GOG Galaxy service while UniverseLAN provides its LAN service. It is an environmental observation, not an SDK guarantee; any other state or record mismatch fails. Requested-peer persona events are retained but excluded from equality after a later official `none`-only variation; arbitrary-user events are filtered out. No raw ID, persona name, avatar data/URL/image ID, count, status text, timestamp, credential, runtime output, control, or relay is recorded. Retrieval has no friendship or direct-message privacy precondition. |
| `Simple/reliable-p2p-listener-peek` characterization | x64 `1.152.11` | Implemented; three clean official-GOG-only trials on 2026-09-14 | Creator creates a public, initially nonjoinable, capacity-two FCM lobby, sets its public collision marker, explicitly observes joinable before discovery, completes a bounded post-arm settling window, and sends one nonempty bounded opaque binary payload with `P2P_SEND_RELIABLE` on the fixed channel. Joiner constructs `GlobalNetworkingListener` before creator release and makes two `PeekP2PPacket` calls for each expected-channel networking callback, using callback-sized private buffers. | The header says `SendP2PPacket` returns scheduling state, recommends bounded packets, and specifies that peek is non-consuming; it directs listener users to peek during notification rather than poll, read, or pop. It does not guarantee delivery from a true send result. All three official trials completed setup and scheduled send, then observed one non-target callback with no expected-channel callback or successful peek pair. Both leaves were terminally confirmed. No raw ID, lobby ID, payload bytes, marker value, or message length is retained. |
| `Simple/reliable-p2p-listener-peek` | x64 `1.152.11` | Implemented; accepted beneficial environmental difference on 2026-09-14 | The characterization facts plus symbolic recipient current-membership/type, callback channel/size relation, callback-local non-consuming peeks, valid non-self creator sender relation, opaque payload/length relation, and listener destruction before shutdown. | Exact same-profile public records require setup, post-arm settling, scheduling, listener lifecycle, terminal leaves, and every callback relation. The manifest accepts only the exact observed pair: official scheduled send with one symbolic non-target callback, no expected-channel callback, and no peeks versus UniverseLAN scheduled delivery with one expected-channel callback and two equivalent successful peeks. This is an environmental observation, not an SDK guarantee; no broader callback allowance exists. Failure artifacts are redacted to traces and report only; controls, configuration, relays, and runtime output are removed. |
| `Simple/bidirectional-reliable-p2p-listener-peek` characterization | x64 `1.152.11` | Implemented; three clean official-GOG-only trials on 2026-09-14 | One public, initially nonjoinable, capacity-two FCM lobby per lane with public collision marker/configuration and explicit observed joinability before filtered list/join. Both members publicly query the other lobby member, construct `GlobalNetworkingListener`, complete the post-arm settling window, and receive the shared release before one distinct `P2P_SEND_RELIABLE` payload per direction on fixed non-default channels. | Official-only and not CTest. Each expected-channel callback performs exactly two callback-local non-consuming peeks only; listener-mode code never polls, reads, or pops. All three trials scheduled both sends, then observed one non-target callback, no expected-channel callback, and zero peeks in each direction. Both listeners were destroyed before shutdown and all terminal fresh-deadline leaves were acknowledged. No raw ID, lobby ID, marker, token, payload byte, or length is retained. |
| `Simple/bidirectional-reliable-p2p-listener-peek` | x64 `1.152.11` | Implemented; accepted beneficial environmental difference on 2026-09-14 | Stable official setup plus symbolic public-peer/current-membership, shared-lobby, distinct-direction channel/payload, listener settling, synchronized release, scheduling, callback-local peek, destruction, and cleanup relations for `user1` to `user2` and independently for `user2` to `user1`. | Every setup, peer/lobby/channel/payload, listener lifecycle, schedule, and cleanup relation is strict. The manifest separately accepts only `scheduled-no-expected-channel-callback-or-peeks` for official versus `scheduled-delivery-and-two-peeks` for UniverseLAN in each direction. It does not assume symmetry: each direction has an independent exact accepted pair, and every other fact fails. |
| `Simple/bidirectional-lobby-message-delivery` characterization | x64 `1.152.11` | Implemented; two official-GOG-only trials on 2026-09-14 | One initially nonjoinable tagged public/capacity-two FCM lobby; explicit marker/capacity updates and observed `SetLobbyJoinable(true)` before discovery; both public `GlobalLobbyMessageListener` instances armed before exchange; one distinct opaque token-derived binary send from each member; callback-local `GetLobbyMessage` only; and fresh-deadline terminal leaves. | Official-only and not CTest. Both trials observed a true send-scheduling result, exactly two callback-local reads per host, one symbolic self sender and one other sender, matching shared-lobby/payload/size relations, and local `self, other` callback order. Cardinality and order remain retained diagnostic evidence; no cross-host or independent-sender global order is asserted. Sanitized artifacts retain no marker, ID, payload, length, timestamp, control, log, or runtime output. |
| `Simple/bidirectional-lobby-message-delivery` | x64 `1.152.11` | Implemented; matched focused live baseline on 2026-09-14 | The stable official setup, listener, dual-send, callback-local read, symbolic sender, directional payload, size, scheduling, and terminal-cleanup relations. | Exact same-profile records require each host's exactly one scheduled send and exactly two callbacks: one self and one other, both from the shared symbolic lobby with callback-local read sender, payload, and message-size relations valid. The callback order is retained but excluded from equality because both sends are independent after the shared gate, so no global sender order is an SDK contract. The focused run observed official `self, other` for both roles; UniverseLAN matched creator order and observed joiner `other, self` while matching every strict relation and cleanup acknowledgement. |
| `Advanced/multiple-lobby-membership-and-message-isolation` characterization | x64 `1.152.11` | Implemented; official-GOG-only characterization succeeded on 2026-09-14 | Two adjacent `CreateLobby` calls with operation-local listeners, automatic enter identity, explicit public/joinable/capacity-two setters and queries, tagged discovery, two adjacent `JoinLobby` calls, and callback-local message reads for symbolic `L0`/`L1` | Not CTest and not a lane comparison. Preserves each host's observed `IMatchmaking` callback order, does not impose cross-host order, and retains only a sanitized official report. The header exposes no lobby-message type, so that unavailable public field is recorded rather than asserted. |
| `Advanced/multiple-lobby-membership-and-message-isolation` | x64 `1.152.11` | Implemented; 2026-09-14 matched live baseline after official order characterization | The characterization facts plus creator’s two back-to-back opaque token-derived sends and both hosts’ callback-local sender/payload/symbolic-lobby isolation checks | Exact same-profile lane comparison preserves ordered callback records. The recorded joiner `L0`/`L1` cross-lobby message sequence is diagnostic only because two official runs observed opposite orders; it is retained, never sorted, and explicitly reported. This does not weaken create/join issuance or assert cross-host order. Each lane must explicitly observe public, joinable, capacity-two lobbies before join; joiner leaves `L1` then `L0`, followed by creator `L1` then `L0`, within the bounded deadline. No raw IDs, tags, payloads, message IDs, timestamps, or metadata values are recorded. |

No social mutation, stats, storage, cloud, or telemetry scenario is implemented. The peer-information scenario only reads existing approved friend/profile data and never creates friendships or mutates social data. The temporary-lobby matchmaking scenarios create and leave tagged temporary lobbies in every lane. The authorized temporary chat scenario relies on bounded participant exit because the public SDK provides no delete-room API.
