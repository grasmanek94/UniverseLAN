# BehaviourTests Progress And TODO

This is the status register for documentation and implementation planning. It
does not turn a characterization or a candidate mismatch into a passing result.
See [COVERAGE.md](COVERAGE.md) for exact contracts and
[BEHAVIOUR_DIFFERENCES.md](BEHAVIOUR_DIFFERENCES.md) for live observations.

## Completed

| Item | Status and result |
| --- | --- |
| Listener P2P peer configuration | The local-only channel-8 `msvc-18` peer overlay now propagates from `UNIVERSELAN_BEHAVIOUR_TEST_GOG_PEER_OVERLAY` into every registered CTest command and stages over the default official `GalaxyPeer64.dll`; with no cache value, staging is unchanged. The verified 2026-09-14 run confirmed the expected hash in both official hosts but completed 18/21 because all three listener contracts retained the historical non-target callback. GOG documents that modern SDKs load the peer from installed client redistributables, so side-by-side staging does not prove runtime selection. Exact delivery remains unresolved and no no-delivery pair is accepted. |
| Framework | Implemented for x64 `1.152.11`: separate official/UniverseLAN hosts, concurrent or sequential lanes, isolated roots, credentials preflight, trace normalization, comparison reports, and opt-in CTest registration. |
| Feature-gated game-server P2P | `game-server-p2p-sender-identity` is generic across selected builds exposing local factory initialization, server networking, and polling APIs. It records only public sender relations and is omitted when those APIs are absent. |
| Non-mutating identity scenarios | `initialize-and-sign-in`, `session-id-repeatability`, and `gog-services-state` are implemented. The service-state contract accepts only official `undefined` versus UniverseLAN `connected`; all other records remain strict. |
| Temporary lobby lifecycle | `public-lobby-create-list-join-leave` is implemented and had a matched live baseline on 2026-09-13. Official list convergence took two bounded attempts and UniverseLAN one; this is an accepted timing/convergence difference, not exact cross-lane equality. |
| Public lobby string filtering | `public-lobby-string-filtering` is implemented with official 2026-09-14 equality-filter characterization evidence and a focused four-host baseline. User1 sequentially creates two initially nonjoinable public capacity-two FCM lobbies, configures distinct opaque marker-derived values under one public key, then explicitly makes both joinable. Both lanes require symbolic target appearance, predicate match, target-only selection/join, two-member/non-self-owner state, and lifecycle cleanup. Stable official GOG exclusion of the unmatched candidate remains strict. UniverseLAN's intentionally relaxed public filter may return a superset; symbolic unmatched-candidate presence/exclusion remains diagnostic only. The current two-lobby variant observed target-only results in UniverseLAN, so it does not itself demonstrate that superset. This does not allow malformed filtering or an unmatched selection/join. |
| Public lobby numerical filtering | `public-lobby-numerical-filtering` passed its first opt-in x64 `1.152.11` two-lane run on 2026-09-16 (9.76 seconds). It uses the safe two-lobby FCM lifecycle with fixed distinct numeric properties plus one shared token-derived run marker. The joiner combines `AddRequestLobbyListNumericalFilter(..., LOBBY_COMPARISON_TYPE_EQUAL)` with that run-marker filter and requires target/predicate/absence-of-run-owned-numerical-nonmatch/target-only-join/two-member-non-self-owner/cleanup relations. The unmatched boolean and retry remain excluded cross-lane diagnostics; no product difference was observed, and no numeric or marker values, IDs, counts, indexes, timestamps, or runtime data were retained. |
| Nonjoinable public-lobby behavior | `public-lobby-not-joinable-behavior` is implemented with two official-GOG-only characterizations followed by a focused strict four-host pass on 2026-09-14. Both official trials exposed no token-filtered candidate after explicit public `SetLobbyJoinable(false)` configuration, so no join was issued. The strict contract keeps that stable list-hidden branch only: no direct-ID join, member/member-data, or send calls; creator remains sole owner/member; terminal leave and post-delete absence are acknowledged. UniverseLAN now excludes nonjoinable lobbies from normal list results. |
| Full-lobby join failure | `public-lobby-full-join-failure` is implemented with two official-only characterizations followed by a focused strict four-host pass. The stable terminal observation is `full` with no entry after one filtered full-lobby list candidate and one join attempt. The creator remained sole owner/member, then acknowledged only its matching terminal leave after a fresh cleanup deadline; the nonmember joiner completed after the post-empty absence probe. |
| Owner-close lobby lifecycle | `public-lobby-owner-close-lifecycle` is implemented with two official-only characterizations followed by a strict four-host matched baseline. Official GOG empirically reported creator `user-left`, joiner `lobby-closed`, tagged-list absence, and no targeted prior-owner member-state callback; UniverseLAN now matches that narrow FCM-close behavior. The header generically documents member notifications, so the absence remains an empirical compatibility fact. Final callback presence/absence follows all scenario pumps plus a bounded settle window; cleanup ack requires a confirmed terminal leave or safe confirmed retry. |
| Owner ownership transition | `public-lobby-owner-ownership-transition` is implemented with two official-only characterizations and a focused strict four-host pass on 2026-09-14. Both official trials observed creator and joiner local `user-left`, prior-owner `LEFT`, owner-change-to-joiner, owner-only membership/owner cache, promoted-owner data authorization, and post-empty filtered-list absence. Cross-listener ordering was identical in those trials but remains diagnostic because listener classes have no ordering guarantee; list attempts varied and remain diagnostic. |
| Temporary lobby data | `public-lobby-data-propagation` is implemented, characterized, and recorded as a matched live baseline. Callback multiplicity and cross-host gate discovery order remain diagnostic only. |
| Advanced multiple-lobby membership | `multiple-lobby-membership-and-message-isolation` is implemented. The first 2026-09-14 official-GOG-only characterization and later comparison supported two adjacent create requests, explicit public/joinable/capacity-two setup, two tagged discoveries and adjacent join requests, symbolic isolated messages, and reverse joiner-first cleanup. The first strict focused comparison exposed opposite official/UniverseLAN cross-lobby joiner message sequences; a second official characterization showed the official sequence itself is not stable, so the retained sequence is diagnostic rather than equality-required. |
| Temporary chat delivery | The strict and characterization paths are implemented. The 2026-09-14 official-only characterization and strict four-host comparison matched the symbolic room-request, send, and callback-local receive path. The approved official profiles must remain friends with messaging privacy permitting friends as an external test-environment precondition; this does not assert a product root cause. |
| Bidirectional temporary chat delivery | `bidirectional-chat-room-message-delivery` is implemented with two clean official-only characterizations, a focused strict four-host CTest, and a full 16-test portable behavior-suite pass on 2026-09-14. Both listeners armed before readiness, user1 initiated and user2 resolved the same public two-member room relation, and each sent exactly once after the shared-room release. Each host validated one remote directional callback-local chat/type/token-size relation. Official self echoes were present, but callback batching/cardinality and local self/other order varied; token-mismatched existing-room messages are also callback-local diagnostics only. Only stable role-local `IChat` facts are strict. UniverseLAN now publishes a created room only after adding both members. Bounded host exit is the agreed cleanup and all relays/artifacts are removed or redacted. |
| Friends peer information | `friends-peer-information-retrieval` is implemented with official-only characterization and strict comparison. Three clean official-only trials on 2026-09-14 matched terminal success, available information, nonempty copied name, symbolic `offline` peer state, and filtered requested-peer `name-avatar`, then `none` persona-change events for both profiles. A later official `none`-only variation makes that event sequence diagnostic. The focused full two-lane CTest accepts only observed `GOG offline` versus `UniverseLAN online` persona state, consistent with the documented service-state environment; it is an accepted beneficial environmental difference, not an SDK guarantee. Terminal success, availability, copied-name, callback-peer, and every other role-local fact remain strict. Retrieval has no friendship or direct-message privacy precondition. |
| Reliable listener P2P peek | `reliable-p2p-listener-peek` is implemented with an official-only diagnostic. Three clean 2026-09-14 official trials observed one symbolic non-target callback and no expected-channel two-peek relation, while UniverseLAN completed the expected callback and two peeks. The verified local overlay still retained the official non-target callback, so the strict exact-delivery contract failed in the 18/21 suite and remains unresolved; no no-delivery pair is accepted. |
| Reliable P2P after lobby leave | `reliable-p2p-after-lobby-leave` is implemented with two official-only characterizations followed by a focused strict four-host pass on 2026-09-14. User2 confirms local leave but remains alive with `GlobalNetworkingListener`; user1 observes targeted member `left`, settles one-member/self-owner public membership, then schedules one reliable opaque send to the former valid user ID. Both official trials and both comparison lanes observed zero networking callbacks and zero peeks after leave. This is a matched baseline, not an accepted LAN benefit; listener-mode polling/read/pop remains absent and both temporary-lobby cleanup acknowledgements completed. |
| Bidirectional reliable listener P2P peek | `bidirectional-reliable-p2p-listener-peek` is implemented with three official-only characterizations. Both members use public membership, settled `GlobalNetworkingListener`, and one reliable payload per direction. The verified local overlay still produced the official non-target callback with no expected-channel callback or peek, while UniverseLAN delivered both directions with two callback-local peeks. The strict exact-delivery contract failed in the 18/21 suite and remains unresolved; no no-delivery pair is accepted. |
| Bidirectional reliable P2P poll/read | `bidirectional-reliable-p2p-poll-read-characterization` completed three clean official-only runs on 2026-09-14, followed by a focused strict four-host pass. Both directions used the listener-free public polling path after synchronized reliable sends: each polling `ProcessData` was followed by `IsP2PPacketAvailable`, then one exact-size `ReadP2PPacket` only when available, followed by immediate empty expected-channel availability. Public peer/payload/sender relations and terminal cleanup held in all runs. No `INetworkingListener` was constructed. Listener-mode P2P tests remain separate unresolved exact-delivery contracts. |
| Bidirectional unreliable listener P2P peek | `bidirectional-unreliable-p2p-listener-peek` is implemented with three official-only characterizations. Both members use public membership, settled `GlobalNetworkingListener`, and one `P2P_SEND_UNRELIABLE` payload per direction. The verified local overlay still produced the official non-target callback with no expected-channel callback or peek, while UniverseLAN delivered both directions with two callback-local peeks. The strict exact-delivery contract failed in the 18/21 suite and remains unresolved; no no-delivery pair is accepted. A true UDP-like send result reports scheduling, not delivery. |
| Bidirectional lobby messages | `bidirectional-lobby-message-delivery` is implemented with two official-only characterizations and a focused strict four-host CTest on 2026-09-14. Both official trials observed one scheduled opaque binary send per member, two callback-local `GetLobbyMessage` reads per host, symbolic self/other sender and shared-lobby relations, matching directional payload/size relations, and local `self, other` order. The focused comparison matched all strict relations and cleanup acknowledgements; UniverseLAN's joiner observed `other, self`. That retained difference is independent-sender order diagnostic context, not a global-order compatibility requirement. |
| Bidirectional lobby member data | `bidirectional-lobby-member-data-propagation` is implemented with two official-only characterizations and a focused strict four-host CTest on 2026-09-14. Both members established public two-member state, armed `GlobalLobbyDataListener` before a shared release, and made one distinct-key opaque-value `SetLobbyMemberData` call. The selected header's void call was completed through `ILobbyMemberDataUpdateListener`. Both official trials and the focused comparison matched successful self-targeted operation terminals, symbolic self/other callback targets, callback-local changed-data/private-equality facts, settled self/other copies, and terminal cleanup. Callback count and local target order remain per-host diagnostics, never cross-host order requirements. |
| Custom networking loopback | `custom-networking-loopback-roundtrip-close` has a public `ICustomNetworking` host probe and runner-owned ephemeral binary echo endpoint. Two initial official-only trials on 2026-09-14 had open failures. A later symbolic URL-form diagnostic observed both valid WebSocket forms fail asynchronously with `connection-failure` and no synchronous API error, while the omitted-scheme diagnostic returned synchronous `invalid-argument` with no bounded terminal callback. No send, data, or close fact was characterized; the strict CTest remains intentionally unregistered. |
| Diagnostics | Explicit runner modes exist for GOG-service state, public-lobby data propagation and string filtering, chat-message delivery, reliable/unreliable listener P2P, custom networking, and official-GOG-only lobby close, ownership transition, and multiple-lobby membership. They collect public observations without deciding lane equality. |
| Indexed matchmaking accessors | Extended `public-lobby-create-list-join-leave` with callback-local `GetLobbyByIndex`, in-range `GetLobbyMemberByIndex`, `GetLobbyDataByIndex`, and `GetLobbyMemberDataByIndex` observations. The creator writes one temporary marker-derived member-data value and both roles retain only symbolic validity, local-member-at-index-zero, repeat-read, and indexed-value-found relations. The focused four-host comparison passed on 2026-09-16; a second official characterization remains required before treating the indexed local-member order as stable. |
| Connection type self relation | Extended the listener-free `bidirectional-reliable-p2p-poll-read` probe with public `INetworking::GetConnectionType(self)`. The normalizer requires the header-documented `CONNECTION_TYPE_DIRECT` result; the current peer enum is retained as sanitized diagnostic context and excluded from cross-lane equality. The focused four-host run matched on 2026-09-16. |
| Stats retrieval and achievement count | Added the non-mutating `stats-retrieve-achievements-number` two-lane characterization and the strict `stats-retrieve-self-callback` contract. After canonicalizing UniverseLAN's self callback ID, two official characterizations and the focused two-lane CTest passed on 2026-09-16 in 1.37 seconds. The count query is retained only as completed-after-retrieval; count, names, descriptions, and catalog contents are title configuration and excluded from lane comparison. |
| Initial shared-file storage state | Added `storage-downloaded-shared-file-count`, a non-mutating strict `IStorage` contract. After sign-in and before any shared-file operation, both lanes reported available storage and zero open downloaded shared files; the focused two-lane CTest passed on 2026-09-16 in 1.19 seconds. |
| Current game language | Added the read-only `current-game-language` `IApps` characterization and strict contract. Two characterizations and the focused two-lane CTest passed on 2026-09-16 in 1.21 seconds: both profiles in both lanes exposed `IApps`, a non-null result, and no ASCII uppercase character. Language text and pointer values remain excluded as title/environment configuration. |
| Current game language copy | Added a two-form, read-only `current-game-language-copy` characterization using only transient one-byte and 256-byte non-null buffers. Two 2026-09-16 runs consistently observed official `runtime-error` with unchanged buffers and UniverseLAN no error with changed/terminated buffers for both profiles. This is a candidate difference only: the header does not document buffer capacity, truncation, or termination behavior, so no strict CTest or implementation change is justified. |
| Current game language code | Added read-only pointer and copy characterizations for `GetCurrentGameLanguageCode` and `GetCurrentGameLanguageCodeCopy`. Two 2026-09-16 runs consistently observed both lanes provide the documented code shape, while official reported `runtime-error` and UniverseLAN no error. The copy form repeated the one-byte/256-byte candidate buffer difference. Both remain characterization only; text, pointers, and buffer contents are excluded. |
| IApps DLC methods | `IsDlcInstalled`, `IsDlcOwned`, and its completion callbacks require a valid DLC `ProductID`; no public title-neutral fixture is available. Do not invent an ID or use base-game ID `0`, which the header reserves only for language queries. |
| Overlay state | Added read-only `IUtils::GetOverlayState` characterization. Two 2026-09-16 runs consistently observed official `undefined` and UniverseLAN `disabled`, with available `IUtils` and no error for both profiles. This is environment-dependent candidate evidence only; do not invoke visibility or control methods unless their documented overlay-initialization precondition is established. |
| Initial file storage state | Added read-only `IStorage::GetFileCount` characterization. Two 2026-09-16 runs consistently observed available storage and no error, with official nonempty and UniverseLAN empty for both profiles. This is storage-state candidate evidence only; file counts, names, paths, and content remain excluded and no storage mutation is performed. |
| IStorage header | Completed selected `1.152.11` `IStorage.h` inventory in `InterfaceBehaviourTestPlans/IStorage.h/HEADER_COVERAGE.md`. `GetDownloadedSharedFileCount` remains strict; `GetFileCount` and `FileExists` have non-mutating characterizations. Every remaining method is explicitly fixture-, mutation-, callback-, historical-, or lifecycle-bound; no file names, IDs, indexes, contents, or persistent storage mutations are invented. |
| IUtils header | Completed selected `1.152.11` `IUtils.h` inventory in `InterfaceBehaviourTestPlans/IUtils.h/HEADER_COVERAGE.md`. GOG-services state/listener and overlay-state coverage already exist. Image, notification, and overlay-control methods are fixture-, runtime-, or precondition-blocked; the current legal notification registration forms exit before trace output in both lanes and are not classified as a product difference. |
| IStats header | Completed selected `1.152.11` `IStats.h` inventory in `InterfaceBehaviourTestPlans/IStats.h/HEADER_COVERAGE.md`. Existing self-retrieval coverage excludes title catalog data. Current terminal-only leaderboard and default-self time-played request forms exit before trace output in both lanes and are runtime-blocked; catalog, stat, leaderboard, and mutation methods remain title-fixture- or cleanup-bound. |
| IUser header | Completed selected `1.152.11` `IUser.h` inventory in `InterfaceBehaviourTestPlans/IUser.h/HEADER_COVERAGE.md`. Existing sign-in/self/session coverage is retained. The only safe pending extension is key-free default-self user-data retrieval; token/ticket, platform-authentication, mutation, and session-control APIs remain sensitive-, fixture-, or cleanup-bound. |

## Remaining Work

| Priority | Work |
| --- | --- |
| Maintain | Rerun direct chat only with the approved official profiles as friends and messaging privacy permitting friends. Treat that as an external environment precondition and record a new candidate only if a stable public mismatch recurs. |
| Maintain | Rerun friends peer information with approved signed-in profiles only. Do not create friendships or mutate social data; record a candidate only for a stable public mismatch. |
| Validate indexed accessors | Run a second official-GOG characterization of `Simple/public-lobby-create-list-join-leave` with the expanded in-range member/list/lobby-data/member-data index observations. The first focused four-host comparison passed on 2026-09-16. Do not promote local-member-at-index-zero from the current compatibility contract into a version-independent SDK guarantee without repeatable official evidence. |
| Run numerical lobby filtering | Completed 2026-09-16: the opt-in `Simple/public-lobby-numerical-filtering` two-lane comparison passed in 9.76 seconds with no product difference. |
| Validate achievement unlock state | With title-provided known achievement keys, add a read-only probe that compares only symbolic unlocked/not-unlocked relations after retrieval. Do not compare count, name, description, icon, or other catalog content, and do not mutate unlock state. |
| Validate language-copy candidate | Characterize one additional independently justified non-null `GetCurrentGameLanguageCopy` buffer form in both lanes. Do not use null/zero-buffer forms, retain language bytes, register a strict CTest, or modify UniverseLAN code unless a public contract is established. |
| Maintain | Provision an approved installed GOG Galaxy client redistributable environment before rerunning `reliable-p2p-listener-peek`. Keep `SendP2PPacket` scheduling distinct from delivery, require exact delivery only after it is observed in the official lane, and do not call poll/read/pop APIs or record opaque values. |
| Maintain | Rerun `reliable-p2p-after-lobby-leave` twice against official GOG before changing its strict zero-callback/zero-peek facts. Preserve confirmed leave, public member-state, settled membership, former-ID, and listener-liveness causal gates; do not infer a general post-leave delivery rule or add poll/read/pop calls. |
| Maintain | Provision an approved installed GOG Galaxy client redistributable environment before rerunning `bidirectional-reliable-p2p-listener-peek`. Keep both directions independent, use public lobby membership for peers, require exact delivery only after it is observed in the official lane, and do not call listener-mode poll/read/pop APIs or retain opaque values. |
| Maintain | Rerun `bidirectional-reliable-p2p-poll-read` through its official listener-free diagnostic before changing strict facts. Keep the polling `ProcessData`/availability/read/empty-after-read relation separate from listener-mode semantics, preserve one read only when available, and do not retain opaque values. |
| Maintain | Provision an approved installed GOG Galaxy client redistributable environment before rerunning `bidirectional-unreliable-p2p-listener-peek`. Keep unreliable scheduling distinct from delivery, use public lobby membership for peers, require exact delivery only after it is observed in the official lane, and do not call listener-mode poll/read/pop APIs or retain opaque values. |
| Maintain | Rerun the bidirectional lobby-message official diagnostic at least twice before changing strict cardinality or directional relations. Keep each member to one send and `GetLobbyMessage` callback-local; retain callback order but never convert independent sender order into a cross-host/global-order rule. |
| Maintain | Rerun the bidirectional lobby-member-data official diagnostic at least twice before changing strict callback-target or copy relations. Keep `SetLobbyMemberData` and its operation listener public, use `GetLobbyMemberDataCopy` only callback-locally and after bounded pumping, and retain callback count/order only as per-host diagnostics. |
| Characterize custom networking | Investigate only through public `ICustomNetworking` calls why the runner-owned loopback endpoint produces stable open failures. Rerun the official-only probe after an approved endpoint/environment change; do not add a strict CTest, classify a UniverseLAN difference, or infer send/peek/read/close semantics until successful official open, roundtrip, and close facts are stable. |
| Expand Simple coverage | Characterize and add contracts for matchmaking failure/lifecycle paths, networking, friends, stats, storage, cloud, custom networking, apps, telemetry, and utils only when account/application mutation and cleanup rules are approved. |
| Add Advanced coverage | The first Advanced lobby scenario is complete. Add further Advanced scenarios only after their constituent Simple public contracts are characterized and stable. |
| Version/platform matrix | Expand beyond x64 `1.152.11` only after repeatable official comparisons establish the baseline. |
| Hygiene | Keep scenario manifests, the plan, coverage, this register, and the difference log synchronized after every live run or characterization. |

## Required Validation

Before documenting a new or changed scenario:

1. Confirm the host uses only public selected-version SDK APIs and that traces omit credentials, private values, raw IDs, timestamps, and runtime output.
2. Configure and build the x64 `1.152.11` framework with `BUILD_UNIVERSELAN_BEHAVIOUR_TESTS=ON` and `LIMIT_VERSIONS="1.152.11"`.
3. Run the appropriate explicit diagnostic before defining or changing a strict public contract; preserve only its sanitized local report reference.
4. With approved credentials, runtime artifacts, authenticated official environment, and network access, configure with `ENABLE_UNIVERSELAN_GOG_BEHAVIOUR_TESTS=ON` and run the focused CTest label through `RunBehaviorCTest.cmake`.
5. Verify cleanup: all temporary-lobby hosts acknowledge cleanup; chat hosts complete bounded exit. Treat retained failure roots as sensitive and inspect them locally only.
6. Update `COVERAGE.md`, `BEHAVIOUR_DIFFERENCES.md`, and `BEHAVIOUR_TEST_PLAN.md` with the actual result and classification, then run `git diff --check`.
